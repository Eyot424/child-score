# 徐瑾熙的星星 · 网页方案设计

> 规则见 [DESIGN.md](DESIGN.md)（v5）
> 本文档：技术与交互方案，未开发

---

## 1. 概述

| 项 | 内容 |
|---|---|
| 名称 | **徐瑾熙的星星** |
| 用户 | 家长（录入）+ 徐瑾熙 6 岁（查看、参与结算） |
| 核心动作 | **每天睡前手动录入一次**，约 30 秒 |
| 主设备 | 手机（竖屏），加到主屏当 App 用 |
| 主题 | 哆啦A梦配色风格 + 可自定义背景图 |
| 里程碑 | **10 / 20 / 60**（已定） |

### 设计目标（按优先级）

1. **录入要快** —— 30 秒内完成，否则坚持不下来
2. **孩子看得懂** —— 不识字也能看明白进度
3. **数据不会丢** —— 单机也要有可靠备份
4. **规则可调** —— 阈值、价格全部做成配置项，两周复盘后要改

---

## 2. 技术方案

### 推荐：单文件 HTML + localStorage

| | |
|---|---|
| 形态 | 一个 `.html` 文件，内联 CSS / JS，无构建、无依赖 |
| 存储 | `localStorage`，JSON 序列化 |
| 部署 | 双击打开，或放到手机上「添加到主屏幕」 |
| 备份 | 一键导出 JSON / 导入 JSON |
| 成本 | 零部署、零服务器、零月费 |

**为什么不用框架**：整个应用的状态就是一个 JSON 对象，页面不超过 5 屏。
原生 JS 几百行足够，而且十年后还能打开——这是要用好几年的东西。

### 决策点：要不要多设备同步？ **[?]**

`localStorage` 的数据**只存在录入的那台设备上**。

| 方案 | 说明 | 适合 |
|---|---|---|
| **A · 纯本地** | 一台手机录入，导出 JSON 备份 | 只有你一个人录入 |
| **B · 在线托管** | 部署成网页，数据存云端，手机电脑都能开 | 爸爸妈妈都要录入 |

规则文档 §8 强调过「爸爸妈妈规则一致」。如果两个人都会记账，**方案 B 更合适**，
否则会出现两台手机数据不一致——这比不做还糟。

**建议**：先做方案 A，但**数据层抽象成 `Storage` 接口**（`load()` / `save()`），
后面换成云端只要改这一个模块，页面代码一行不动。

---

## 3. 页面结构

底部四个 Tab，全部单页切换，无路由。

```
┌─────────────────────────┐
│   徐瑾熙的星星           │  ← 顶栏：⭐ 23   ☀️ 14
├─────────────────────────┤
│                         │
│      （内容区）          │
│                         │
├─────────────────────────┤
│  今天  日历  收集册  商店 │  ← 底部 Tab
└─────────────────────────┘
```

### 3.1 「今天」—— 主录入页

```
┌─────────────────────────┐
│   8月10日 星期日         │
│                         │
│  ┌────┐ ┌────┐ ┌────┐  │
│  │ 🪥 │ │ 🛏 │ │ 📖 │  │   6 个大方块
│  │刷牙│ │睡觉│ │学习│  │   点一下打勾
│  │ ✓  │ │ ✓  │ │    │  │   再点取消
│  └────┘ └────┘ └────┘  │
│  ┌────┐ ┌────┐ ┌────┐  │
│  │ 🧹 │ │ 😊 │ │ 🍚 │  │
│  │干活│ │脾气│ │吃饭│  │
│  │ ✓  │ │ ✓  │ │ ✓  │  │
│  └────┘ └────┘ └────┘  │
│                         │
│      今天 5 颗星 ⭐      │
│   ☀️ 拿到太阳了！        │
│                         │
│  ─── 今天发生了 ───      │
│  [ 情绪失控 ]  [ 打人 ]  │   出事才点，平时不用碰
│                         │
│  ┌───────────────────┐  │
│  │      结  算        │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

**关键交互**

- 6 个格子**默认全部未勾**，做到才点亮 —— 避免「默认给分再扣」的心理暗示
- 星数和太阳状态**实时预览**，点完就知道今天够不够
- 「情绪失控 / 打人」是两个不显眼的次要按钮，**不做成大红色**
- 「小脾气」不需要按钮 —— 直接不勾「管好脾气」那格就是了
  （但结算时弹一句「今天有小脾气吗？」，勾了才计入统计，用于复盘报表）
- 点「结算」→ 星星飞进罐子的动画 → 够 5 颗则月历上今天盖太阳
- **结算后当天锁定**，长按 2 秒才能重新编辑

### 3.2 「日历」—— 月视图

```
┌─────────────────────────┐
│  ◀   2026 年 8 月   ▶   │
│  日 一 二 三 四 五 六     │
│                 1  2    │
│  3  4  5  6  7  8  9    │
│ ☀️ ☀️ 4⭐ ☀️ ☀️ 💧 ☀️    │
│ 10 11 12 13 14 15 16    │
│ ☀️ ·  ·  ·  ·  ·  ·     │
└─────────────────────────┘
   本月 ☀️ 11 个   ⭐ 均 4.6
```

| 状态 | 显示 |
|---|---|
| 得到太阳 | ☀️ |
| 有星星没太阳 | `3⭐` 小字 |
| 打人日 | 💧（雨滴，不用 ❌ —— 不做成羞辱标记） |
| 未录入 | 灰点 `·` |
| 今天 | 蓝色边框高亮 |

- **点任意过去的日期 → 补录或修改**（经常会忘了录，必须支持）
- **未来日期不可点**
- 底部显示本月统计

### 3.3 「收集册」—— 孩子最爱看的一屏 ⭐

这是月历**做不到**的事：「第 10 个太阳换玩具」跟日期无关，
你不可能预先知道第 10 个太阳落在哪天，所以月历上画不了奖品格子。

**60 格收集卡**，每得一个太阳点亮一格：

```
┌─────────────────────────┐
│   还差 6 个太阳就有玩具！ │
│                         │
│  ☀️☀️☀️☀️☀️☀️☀️☀️☀️ 🧸  │  1-10
│  ○ ○ ○ ○ ○ ○ ○ ○ ○ 🧸🎡 │  11-20
│  ○ ○ ○ ○ ○ ○ ○ ○ ○ 🧸  │  21-30
│  ○ ○ ○ ○ ○ ○ ○ ○ ○ 🧸🎡 │  31-40
│  ○ ○ ○ ○ ○ ○ ○ ○ ○ 🧸  │  41-50
│  ○ ○ ○ ○ ○ ○ ○ ○ ○🧸🎡✈️│  51-60
└─────────────────────────┘
```

**奖品格标记**

| 格子 | 解锁 |
|---|---|
| 10 · 30 · 50 | 🧸 玩具 |
| 20 · 40 | 🧸 玩具 + 🎡 游乐场 |
| 60 | 🧸 玩具 + 🎡 游乐场 + ✈️ **旅游** |

- 一轮 60 格，填满后开新一轮，历史轮次可回看
- 顶部一句大字：**「还差 N 个太阳就有玩具！」** —— 不识字也看得懂格子
- 点亮新格子时有动画，走到奖品格时全屏庆祝

### 3.4 「商店」—— 兑换

```
┌─────────────────────────┐
│      ⭐ 23 颗星星        │
│                         │
│  ┌───────────────────┐  │
│  │ 📱 玩 20 分钟     │  │
│  │              1 ⭐ │  │  可换 → 亮色
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ 🍭 零食           │  │
│  │             10 ⭐ │  │
│  └───────────────────┘  │
│  ┌───────────────────┐  │
│  │ ☀️ 补一个太阳      │  │
│  │  本周还能补 1 次   │  │
│  │             20 ⭐ │  │
│  └───────────────────┘  │
│                         │
│  ─── 已解锁待兑现 ───    │
│  🧸 玩具 ×1  [ 已给她 ]  │
└─────────────────────────┘
```

- 星星不够的项目**变灰但仍然显示**（看得见目标才有动力）
- 兑换需二次确认（防误点）
- **「已解锁待兑现」区**：里程碑解锁 ≠ 东西已经买了。
  旅游要订机票，玩具要去买 —— 需要一个「已兑现」的状态标记

### 3.5 设置（藏在顶栏齿轮里）

| 分组 | 内容 |
|---|---|
| 规则 | 6 个格子的名称/图标、太阳门槛（默认 5）、里程碑阈值（10/20/60） |
| 价格 | 三个兑换项的星数、每周限制 |
| 基线期 | **开关 + 结束日期**，开启时扣分只记录不生效 |
| 外观 | 上传背景图、亮/暗主题 |
| 数据 | 导出 JSON / 导入 JSON / 清空 |

---

## 4. 数据模型

```js
{
  version: 1,
  child: { name: "徐瑾熙", startDate: "2026-08-11" },

  config: {
    slots: [
      { id: "teeth",   label: "刷牙",       icon: "🪥" },
      { id: "sleep",   label: "按时睡觉",   icon: "🛏" },
      { id: "study",   label: "认真学习",   icon: "📖" },
      { id: "chore",   label: "帮忙干活",   icon: "🧹" },
      { id: "temper",  label: "管好脾气",   icon: "😊" },
      { id: "meal",    label: "自己好好吃饭", icon: "🍚" }
    ],
    sunThreshold: 5,
    penaltyEnabled: false,          // 基线期开关
    baselineEndDate: "2026-08-24",
    starRewards: [
      { id: "screen", label: "玩 20 分钟", cost: 1,  weeklyLimit: null },
      { id: "snack",  label: "零食",       cost: 10, weeklyLimit: 2    },
      { id: "sunbuy", label: "补一个太阳", cost: 20, weeklyLimit: 1    }
    ],
    milestones: [
      { id: "toy",  label: "玩具",   threshold: 10, icon: "🧸" },
      { id: "park", label: "游乐场", threshold: 20, icon: "🎡" },
      { id: "trip", label: "旅游",   threshold: 60, icon: "✈️" }
    ]
  },

  // 每日记录，key = YYYY-MM-DD
  days: {
    "2026-08-10": {
      slots: { teeth:true, sleep:true, study:false,
               chore:true, temper:true, meal:true },
      tantrum: false,        // 小脾气（仅统计用，已体现在 temper 格）
      meltdown: false,       // 情绪失控
      hit: false,            // 打人
      bonusStars: 0,         // 惊喜星
      settled: true
    }
  },

  // 兑换与人工调整的流水（每日星星不入账，由 days 推导）
  ledger: [
    { date:"2026-08-09", type:"redeem", itemId:"snack", stars:-10 },
    { date:"2026-08-12", type:"buysun", stars:-20, suns:+1 }
  ],

  // 里程碑已发放次数（防重复发放）
  milestoneGrants: { toy: 1, park: 0, trip: 0 },
  // 已实际兑现（东西真的给她了）
  milestoneFulfilled: { toy: 1, park: 0, trip: 0 },

  ui: { backgroundImage: null, theme: "auto" }
}
```

---

## 5. 核心计算逻辑

### 关键设计决定：余额永远全量重算，不存增量

> **`starBalance` 和 `sunTotal` 都是派生值，不落库。**
> 每次渲染从 `days` + `ledger` 从头算一遍。

原因：日历支持**修改历史日期**。如果余额是累加存下来的，改一次 8 月 3 日的记录，
后面所有余额全错，而且错得无声无息。全量重算彻底消灭这类 bug——
一年 365 条记录，重算耗时不到 1 毫秒。

### 单日结算

```js
function settleDay(day, config) {
  const enabled = config.penaltyEnabled;

  // 1. 星星
  let stars = countTrue(day.slots) + (day.bonusStars || 0);
  if (enabled && day.hit) stars = 0;          // 打人 → 清零
                                               // 情绪失控 → 星星保留

  // 2. 太阳
  let sun = stars >= config.sunThreshold;
  if (enabled && (day.meltdown || day.hit)) sun = false;

  // 3. 打人额外扣 1 个已有太阳
  const sunPenalty = (enabled && day.hit) ? 1 : 0;

  return { stars, sun, sunPenalty };
}
```

### 全局汇总

```js
function computeTotals(data) {
  let stars = 0, suns = 0;

  for (const date of Object.keys(data.days).sort()) {
    const r = settleDay(data.days[date], data.config);
    stars += r.stars;
    suns  += (r.sun ? 1 : 0) - r.sunPenalty;
  }
  for (const e of data.ledger) {
    stars += e.stars || 0;
    suns  += e.suns  || 0;
  }

  return { stars: Math.max(0, stars), suns: Math.max(0, suns) };  // 都不为负
}
```

### 里程碑解锁

```js
function checkMilestones(suns, config, grants) {
  const newly = [];
  for (const m of config.milestones) {
    const earned = Math.floor(suns / m.threshold);   // 每满 N 个解锁一次
    if (earned > (grants[m.id] || 0)) {
      newly.push({ ...m, count: earned - grants[m.id] });
    }
  }
  return newly;
}
```

**注意**：`milestoneGrants` **只增不减**。
如果修改历史导致太阳数下降，已发放的不回收（东西已经给出去了），
只是要多攒一些才能触发下一次——这个行为要在代码里写注释说明，不是 bug。

---

## 6. 视觉设计

### 版权说明

**哆啦A梦的角色形象受版权保护，页面不会绘制或内嵌该形象。**

替代方案是**用它的视觉语言**——配色和造型元素不受版权限制，
再加一个「上传背景图」功能，你自己的图片存在本地，想放什么放什么。

### 配色

| 用途 | 色值 | 说明 |
|---|---|---|
| 主色 | `#00A0E9` | 标志性的那个蓝 |
| 底色 | `#FFFFFF` | 白肚皮 |
| 强调 | `#E60012` | 项圈红 |
| 星星 | `#FFD900` | 铃铛黄 |
| 次要 | `#F19EC2` | 任意门粉 |
| 描边 | `#1A1A1A` | 粗黑描边，卡通感的关键 |

### 造型语言

| 元素 | 用法 |
|---|---|
| **铃铛** | 星星罐的罐子做成铃铛形；加载动画是摇晃的铃铛 |
| **四次元口袋** | 半圆形卡片，商店的兑换项从「口袋」里冒出来 |
| **竹蜻蜓** | 结算成功时的旋转动画 |
| **任意门** | 里程碑解锁时的粉色门打开动画 |
| **粗描边 + 大圆角** | 全局造型基调，3px 黑描边 + 16px 圆角 |

### 字体与尺寸

- 中文用系统圆体（`PingFang SC` / `Microsoft YaHei`），字重偏粗
- **格子按钮最小 88×88 px** —— 6 岁孩子的手指精度
- 数字（星星数、太阳数）用超大字号，孩子要能一眼看到

### 背景图

- 设置里上传，转 base64 存 `localStorage`
- 自动加一层半透明白色遮罩（保证文字可读）
- 遮罩透明度可调

---

## 7. 边界情况

| 情况 | 处理 |
|---|---|
| 忘了录入，第二天补 | 日历点过去任意日期即可补录 |
| 修改已结算的历史日期 | 长按 2 秒解锁；余额全量重算，自动同步 |
| 未来日期 | 不可点击 |
| 修改历史导致太阳数下降到已发奖之下 | 已发放数不回退，下次要多攒（代码注释说明） |
| 兑换时星星不足 | 按钮置灰，不可点 |
| 「补太阳」超出每周 1 次 | 显示「本周已用完」，下周一重置 |
| 打人扣太阳导致总数为负 | 归零，不欠账 |
| 基线期内触发扣分 | 记录、进统计、进报表，**但不生效** |
| 基线期结束当天 | 首次打开时弹窗提醒 + 展示两周报表 |
| 孩子自己点星星 | **家长锁**：录入区默认锁定，长按 2 秒解锁 |
| 数据丢失 | 每次修改后自动生成一份导出快照；设置里可手动导出 JSON |

### 家长锁

6 岁孩子一定会自己去点格子。但结算时又该让她参与（规则文档 §9）。

**做法**：录入区默认锁定 → 家长长按 2 秒解锁 → 孩子自己点格子，家长在旁边确认
→ 点「结算」后自动重新锁上。

不用 PIN 码，长按就够——目的是防误触和防止她偷偷加分，不是真的防破解。

---

## 8. 开发计划

| 阶段 | 内容 | 产出 |
|---|---|---|
| **P0** | 数据层 + 结算逻辑 + 「今天」页 | 能录入、能算对 |
| **P1** | 日历页（含补录）+ 收集册页 | 能看进度 |
| **P2** | 商店页 + 里程碑解锁 | 闭环可用 |
| **P3** | 哆啦A梦主题 + 动画 + 背景图上传 | 孩子愿意看 |
| **P4** | 设置页 + 导出导入 + 复盘报表 | 两周后校准要用 |

**建议一次做完 P0–P3 交付**，P4 可以稍后补。
单文件 HTML，预计 1200~1500 行。

### P4 报表要出的三个数

这三个数直接决定两周后怎么调规则（见 DESIGN.md §5、§9）：

1. **情绪失控频率**（次/周）
2. **「自己好好吃饭」达成率** —— 决定里程碑要不要从 10/20/60 降到 8/16/48
3. **各格达成率** —— 哪一格连续 21 天满勾，就该毕业换新目标了

---

## 9. 待确认 **[?]**

| # | 问题 | 建议 |
|---|---|---|
| 1 | 只有你一个人录入，还是爸妈都要录？ | 两人都录 → 需要在线版；一人录 → 单文件够了 |
| 2 | 背景图你自己提供吗？ | 页面做上传入口，你传什么显示什么 |
| 3 | 要不要家长锁？ | 建议要，长按解锁即可 |
